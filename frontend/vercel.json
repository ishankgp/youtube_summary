{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm install --force && npm install --save-dev typescript @types/node @types/react @types/react-dom && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --force",
  "outputDirectory": ".next",
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
} 