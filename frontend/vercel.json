{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm install --save-dev typescript @types/node @types/react @types/react-dom && npm install --force && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --force",
  "outputDirectory": ".next",
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
} 